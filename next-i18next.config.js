module.exports = {
  i18n: {
    locales: ['en', 'tr', 'de'],  // Desteklenen diller
    defaultLocale: 'en',          // Varsayılan dil (Çevirisi yapılmamış dil olduğunda default olarak gösterilecek dil)
    localeDetection: true,        // Dil algılama aktif mi?
  },
  react: { useSuspense: false }
};